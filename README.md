# chatterkov
A conversational chatbot without neural networks!

## What is this?
Simple. ChatterKov is a conversational chatbot that does not use neural networks. Instead, it uses a very complex vote-based algorithm with multiple "brains".

## How to use?
ChatterKov doesn't actually use any external dependencies! You can simply run `main.py`.
